nav.nav.has-shadow
  .container
    .nav-left
      a.nav-item.logo href="#" OdinBook
    span.nav-toggle
      span
      span
      span
    .nav-right.nav-menu
      - if user_signed_in?
        a.nav-item: em Welcome, #{current_user.name}
        .nav-item
          = link_to "Logout", destroy_user_session_path, method: :delete, class: 'button is-light'